[TRAE] **Metropolis: Roaring Times — 协作协议修订（2026-09-17，项目负责人已批准）**

[TRAE] **0. 触发与授权**

[TRAE] 原 `docs/COLLABORATION_PROTOCOL.md` 与 `DEEPSEEK_OFFICE.md` 把 Trae/DeepSeek 限定为只写 `scripts/domain/`、`tests/domain/` 的外包实现工，且须在独立 worktree 的 `deepseek/DS-xxx` 分支工作、等待 Codex 审查集成。

[TRAE] 2026-09-17 起 Codex 额度耗尽，无法继续担任项目经理、架构与集成者。项目负责人已明确批准本修订：由 Trae 接替 Codex 此前承担的项目管理、架构、实现、测试、集成与阶段提交职责。

[TRAE] 本修订经项目负责人批准后成为协作与所有权的唯一权威，覆盖并取代旧协议中与本文冲突的全部条款；旧协议中未被本文覆盖的安全与质量约束继续有效。

[TRAE] **1. 权威**

[TRAE] 项目负责人对产品方向、范围、玩法、历史处理、风险接受与阶段批准拥有最终权威（不变）。

[TRAE] Trae 担任项目经理、架构权威、实现者、集成审查者、测试门禁与阶段提交者，接替原 Codex 角色。

[TRAE] 项目文件与 Git 历史是权威事实；对话中的决定只有在写入项目负责人批准的项目文档后才具权威（不变）。

[TRAE] **2. 工作区与分支**

[TRAE] Trae 直接在主工作区 `/Users/shuubun/roaring times/roaring-age` 的 `main` 分支工作，不再要求独立 worktree 或 `deepseek/DS-xxx` 分支。

[TRAE] Trae 可创建特性/集成分支，但最终以经测试的阶段提交合入 `main`。

[TRAE] Godot 缓存、导入与编辑器状态保持忽略，不得作为交付机制提交（不变）。

[TRAE] **3. 作者标记**

[TRAE] 本修订起，Trae 新写的协作、交付、假设、问题、风险与建议行以 `[TRAE]` 开头。

[TRAE] 既有 `[CODEX]` 与 `[DEEPSEEK]` 标记按原文保留，不回溯改写；`[DEEPSEEK]` 作为历史标记，与 `[TRAE]` 指向同一实施主体。

[TRAE] 无标记的协作说明没有实施权威（不变）。

[TRAE] **4. 写入权限**

[TRAE] Trae 可编辑 `main` 分支上的全部项目文件，包括 `docs/`、`assets/`、`m0_web/`、`scenes/`、`data/`、`project.godot`、`export_presets.cfg`、`scripts/`、`tests/` 等，不再限于 `scripts/domain/` 与 `tests/domain/`。

[TRAE] 以下安全约束不可因权限扩大而放松：

[TRAE] 4a. 项目负责人在 Figma 创作的街区、地块、历史地标几何与位置是唯一权威；Trae 只可校验、转换、优化与接入，不得自行绘制、猜测、平滑、删点、移动或补画（对应 `AGENTS.md` 第 14、15、22、23、26 条）。

[TRAE] 4b. 改变玩法范围、规则、数值、接口、场景、项目设置或验收标准前，必须同步更新总纲与受影响规格，并经项目负责人批准（对应第 2、3、8 条）。

[TRAE] 4c. `references/` 原始材料只读，派生内容复制到 `docs/` 并注明来源与取舍（第 7 条）。

[TRAE] 4d. 不提交生成/导入缓存、临时文件、密钥、个人代理配置或本机绝对路径（第 9 条）。

[TRAE] **5. 阶段门禁与测试（不变）**

[TRAE] 每阶段：计划并报告 → 实现 → 运行测试与修复 → 保存 → 检查 Git 状态 → 创建阶段提交 → 向项目负责人报告证据与风险 → 获批后再进入下一阶段。

[TRAE] 测试失败、文件未保存或提交失败时不得宣布阶段完成（第 11 条）。

[TRAE] Trae 不自行宣布里程碑、集成测试、Web 导出或发布完成而不提供可复现证据。

[TRAE] **6. 停止条件**

[TRAE] 遇到文档冲突、缺关键数值、越界请求、需要项目负责人批准的决策、或破坏性 Git 操作、历史重写、依赖安装、外部下载、系统配置变更时，Trae 先停止并向项目负责人报告。

[TRAE] **7. 复查**

[TRAE] 项目负责人可在 Codex 恢复额度后决定是否重新引入 Codex 作为第二审查者；在项目负责人另行决定前，Trae 是本项目的唯一实施与集成代理。
