# 《Metropolis: Roaring Times》老板审查版项目管理计划

> 用途：让老板不用阅读代码，也能知道项目现在在哪里、下一步谁做什么、什么情况下必须停下来审批。
>
> 详细技术计划：`07_IMPLEMENTATION_PLAN.md`
>
> Figma 地图交付规范：`MAP_ASSET_PIPELINE.md`
>
> 语言规则：本文件及老板需要审查的开发文档以中文为主。英文仅保留产品名、软件名、文件名、稳定技术标识和玩家实际看到的英文原文。

## 一、当前真实状态

已经完成并提交：

1. 项目总纲、玩法规则、技术架构和验收标准。
2. 可供投资人说明玩法的独立网页灰盒。
3. Godot 4.7.1 环境、最小运行壳、Web 导出和浏览器存储验证。
4. Codex 与 DeepSeek 的分工协议。

尚未完成：

1. 正式游戏地图。
2. 正式分区边界接入。
3. 64 个可购买地块。
4. Godot 正式玩法系统和正式 UI。
5. 正式建筑、人物和音频美术。

当前代码基线仍然只是可运行的 Godot 环境壳。Codex 曾经擅自制作的临时地图和分区没有提交，现已全部删除。

当前文档语言存在一项历史遗留问题：`01_PRODUCT_BRIEF.md` 至 `07_IMPLEMENTATION_PLAN.md` 等早期权威规格仍以英文为主。它们必须按照 `DOCUMENT_LANGUAGE_MIGRATION.md` 分批翻译和校验；在完成前，Codex 向老板提交的阶段计划、风险、决策题和验收摘要一律使用中文。

当前最近交付已经由老板调整为：先使用正式 Figma 地图升级现有 M0 网页，制作给制作人观看的 M1W 地图交互 Demo；M1W 验收前不进入 Godot M1A。

## 二、从现在开始的责任划分

### 老板负责

- 在 Figma 中创作并批准生产底图。
- 在 Figma 中创作并批准 12 个曼哈顿分区边界；12 个稳定 ID 以 `MAP_ASSET_PIPELINE.md` 为准，旧六分区/五玩法区模型已退役。
- 决定地图构图、地理压缩、道路、标签和视觉取舍。
- 审批分区接入效果、64 地块来源、缩放层级、建筑位置和所有视觉样品。
- 对任何范围、规则和美术方向变更作最终决定。

### Codex 负责

- 项目管理、范围控制、风险报告和阶段计划。
- 检查老板提交的 Figma 包是否完整、同尺寸、同坐标和可解析。
- 把老板批准的 SVG 原样转换成 Godot 可用数据。
- 编写 Godot 场景、交互、玩法、测试和 Web 导出。
- 每阶段测试、修错、保存和 Git 提交。
- 不得自行画底图、分区或以占位形状替代缺失的老板资产。

### DeepSeek 负责

- 只处理 Codex 明确分派的纯规则/几何校验代码。
- 不接触 Figma、美术、地图构图、场景和项目设置。
- 其代码必须经过 Codex 审查、复测和老板阶段审批后才能进入主分支。

## 三、老板现在需要交付什么

老板在 Figma 中建立任意尺寸、北向朝上的矢量画布；尺寸不影响游戏缩放，也不作为交付门槛。为了减少手工工作，只需：

1. 使用 `File → Save local copy…` 保存一份版本化 `.fig`。
2. 选中主画框并运行项目插件 `Metropolis Handoff Exporter`；如果当前六个地图顶层图层是并列 Group，则选中其中任意一层，插件会按实际矢量边界自动使用虚拟主画框。
3. 点击“一键导出 ZIP”。
4. 把 `.fig` 和 ZIP 交给 Codex。

插件自动保存图层、位置、颜色、矢量、原始图片以及网页使用的地图、分区、Brand 和对齐预览。安装说明在 `tools/figma_handoff_exporter/README.md`。如果交付不完整，Codex 只报告问题，不修改老板源文件。

## 四、后续阶段怎么走

### 第 0 步：本次管理纠偏

Codex 要做：

- 删除错误的临时地图实现；
- 把 Figma 交付权、任意矢量画布、格式和图层结构贯通到所有文档；
- 运行原有 Godot 测试和文档矛盾扫描；
- 创建一个单独的纠偏 Git 提交；
- 停下来让老板审查。

老板要做：

- 审查本文件和 Figma 交付规范；
- 指出任何责任、顺序或交付要求错误；
- 明确批准后，Codex 才等待/接收 Figma 资产。

### 第 1 步：Figma 一键交付与素材检查（M1W 输入门）

Codex 要做：

- 原样保存老板提交的文件；
- 读取并记录实际画布尺寸，检查文件名、各 SVG `viewBox` 是否一致、12 个权威分区名称、闭合边界、重叠、缺口和对齐；
- 生成一份“哪些通过、哪些有问题”的中文报告；
- 不修改老板源文件。

老板要做：

- 修正被报告的 Figma 问题，或者明确接受某项有意变形；
- 批准素材包可以进入 M1W 网页制作。

完成条件：老板明确说素材接收通过。此时只允许开始 M1W 网页地图代码，不允许启动 Godot M1A。

### 第 2 步：制作人地图网页 Demo（M1W）

Codex 在现有 `m0_web` 中接入正式 Brand、地图底图和分区 SVG，实现拖拽、固定倍率缩放、禁止旋转、分区悬停、点击保持和英文分区详情。保留现有网页的游戏状态、综合操作栏和玩法说明，不擅自制作 64 个正式地块。Chrome 测试并由老板验收后创建独立 Git 提交。

### 第 3 步：Godot 地图对齐与一个分区交互样板（M1A，当前未授权）

Codex 要做：

- 把老板 SVG 无损转成归一化坐标；
- 在 Godot 中显示老板底图；
- 叠加分区边界；
- 只实现一个分区的悬停、点击保持和详情页样板；
- 验证参考图、Figma 源文件和开发资料不进入 Web 发布包。

Codex 不能做：

- 改变分区形状；
- 自行补画缺失区域；
- 开始64地块；
- 制作最终建筑美术。

老板要做：

- 审查 Godot 叠加结果是否和 Figma 对齐；
- 审查边框样式、详情页方向和地图占屏比例；
- 批准后才创建 M1A Git 提交。

### 第 4 步：确定64个可购买地块由谁设计（M1B 前硬决策）

目前尚未决定以下事项：

> 64 个可购买地块的边界，是由老板继续在 Figma 中画，还是由 Codex 在老板分区内提出第一版方案，再交老板逐区审批？

Codex 不得自行选择。老板批准责任方案后，先更新文档并提交，再进入 64 地块生产。

### 第 5 步：64地块、地图相机和完整分区交互（M1B）

根据第3步批准的来源：

- 完成 64 个地块、稳定英文 ID、分区归属和邻接关系；
- 实现放大、缩小、拖拽、边界限制和禁止旋转；
- 实现所有分区悬停/选择；
- 实现远中景分区摘要、近景建筑显隐框架；
- 做几何自动检查，避免自交、重叠、缝隙和无法点击的小地块。

老板审查地图可读性和每个分区的地块安排，批准后提交 M1B。

### 第 6 步：正式界面框架（M2）

Codex 建立：

- 顶部状态栏和 `End Turn`；
- 左侧五页综合操作栏；
- 右侧地产/分区详情；
- 弹窗、提示、帮助和结果页框架；
- 全英文玩家界面。

本阶段不实现真实扣款，先证明布局、地图空间和操作顺序。老板批准交互样板后提交。

### 第 7 步：地产、银行、账本和存档（M3）

实现真实购地、借款、还款、贷款期限、银行接管、经纪出售、账本和版本化存档。所有钱和产权变化必须是原子事务，测试重复点击不能重复扣款或加钱。

### 第 8 步：建筑、租金和经济循环（M4）

实现四类建筑、建造、拆除、120%残值改建、租金、维护、交通加成、污染影响、经济阶段和完整回合结算。

### 第 9 步：三种 AI 对手（M5）

分别实现 Tycoon、Landlady、Shark，但每局只有一个对手。使用固定种子模拟大量对局，证明三种人格行为真的不同，而不是只换头像。

### 第 10 步：股票市场、投资消息和新闻来源（M5F）

实现债券、工业股票、投资信托、交易费用、行动点消耗、历史/虚构/政府消息分类和来源审计。

### 第 11 步：政府拍卖和债务紧急拍卖（M6）

先由 Codex 向老板提交英文拍卖故事和中文说明的数值方案；老板批准故事后再实现竞价、流拍、AI 退出、银行接管与到期债务处置。

### 第 12 步：1916区划法、事件和完整灰盒规则（M7）

实现第10/12回合预警、第14回合生效、第15—16回合过渡、第17回合处罚，以及老板批准的全局/分区事件。完成真正可运行20回合的规则灰盒。

### 第 13 步：投资人灰盒（G1）

把真实玩法打包成最简洁、可以讲解的浏览器版本。允许白底黑框和临时图形，但必须明确标注不是最终美术。

### 第 14 步：美术样品与批量生产（M8A/M8B）

先做少量样品：建筑、AI头像、UI、事件图。每类样品老板批准后才能批量生产。所有公共/私人建筑保持独立素材，并在同一近景阈值显示。

### 第 15 步：最终 QA 与 Web 发布候选（M9）

运行完整20回合模拟、浏览器存档、Chrome/Safari、性能、分辨率、音频、无障碍、素材来源和发布包审计。所有严重问题归零、老板接受剩余问题后，才产生最终发布候选。

## 五、每个阶段固定管理流程

以后每个阶段必须按相同顺序：

1. **开始前报告**：Codex说明本阶段做什么、不做什么、输入、风险和需要老板决定的事项。
2. **老板批准**：只有明确批准当前范围后才开工。
3. **执行中报告风险**：遇到缺文件、规格冲突、越权风险或测试失败立即告诉老板。
4. **完成实现**：只完成当前阶段，不提前铺下一阶段。
5. **运行测试并修错**：失败不能用解释代替通过。
6. **提交老板验收**：视觉、故事或规则需要老板判断时，先展示证据，不自行判定通过。
7. **保存并提交 Git**：老板需要审批的门通过后，创建一个可恢复的 Git 提交。
8. **阶段报告**：列出文件、测试、提交号、剩余风险和下一阶段。
9. **再次等待批准**：不得把“继续讨论”自动理解成批准新的范围或视觉决定。

## 六、Codex 每次向老板汇报的固定格式

```text
当前阶段：
本次完成：
没有做：
测试结果：
Git 提交：
仍有风险：
需要老板决定：
下一步（未授权）：
```

如果没有 Git 提交，必须明确说明为什么没有提交以及文件是否仍安全保存在本地。

## 七、当前需要老板审查的事项

1. 已决定由老板使用 Figma 创作底图和 12 个权威分区，Codex 只做检查、转换和接入。
2. 已决定不限制固定像素尺寸，以同一实际矢量画布和一致 `viewBox` 作为对齐标准。
3. 是否接受 M1A 必须等待完整 Figma 包，禁止占位地理？
4. 64 个可购买地块由谁设计，暂时保持未决定，留在 M1B 前单独审批，是否接受？
5. 是否接受本文件当前阶段顺序和固定管理流程？
